# Local AutoLISP Workspace

This directory is the default local workspace for AutoLISP used by CadGPT Jobs and Skills.

CadGPT local file tools may list, search, read, create, and patch files under `lisp/**`.

Reusable AutoLISP belongs here rather than inside Job folders. Job-specific orchestration belongs under `jobs/**`.
