# CadGPT public loopback endpoint owned by the tiny wake-agent.
HOST=127.0.0.1
PORT=3000

# Full CadGPT MCP wakes only after ChatGPT calls CadGPT.
CADGPT_INTERNAL_PORT=3001
CADGPT_WAKE_POLL_MS=2500
# Full CadGPT core returns to lazy mode after this much MCP inactivity once no request/stream remains.
CADGPT_SESSION_IDLE_MS=120000

# CadGPT user/runtime data root.
# Beta keeps this inside the repository. Packaged builds can point it to an
# absolute per-user path such as %LOCALAPPDATA%\CadGPT without changing tool paths.
CADGPT_APPDATA_ROOT=appdata

# Optional opaque local MCP path token. When set, ChatGPT tunnel targets /mcp/<token>.
MCP_TOKEN=

# OpenAI Secure MCP Tunnel. Filled during setup.
OPENAI_TUNNEL_ID=
OPENAI_TUNNEL_API_KEY=
OPENAI_TUNNEL_HEALTH_PORT=8080

# CAD MCP upstream process. It runs only when BOTH conditions are true:
# 1) ChatGPT has activated CadGPT, and 2) acad.exe is running.
# It is shut down when AutoCAD closes or when the full CadGPT core sleeps.
CAD_MCP_PYTHON=.venv-cad\Scripts\python.exe
CAD_MCP_ENTRY=runtimes\cad-mcp\main.py
CAD_MCP_CONNECT_TIMEOUT_MS=15000

# User Lisp/Job source folders are not configured as generic filesystem roots.
# They are selected explicitly for library_import, copied into AppData, and all
# subsequent CadGPT work uses the managed copy under appdata/libraries/**.
