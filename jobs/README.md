# Jobs

CadGPT Jobs are small, repeatable CAD workflows.

Read `JOB_RULES.md` before creating or editing a Job.

Each Job should live under `jobs/<job-name>/` with its primary definition in `JOB.md`. Reusable AutoLISP implementation belongs under `lisp/**`, not inside Job folders.
