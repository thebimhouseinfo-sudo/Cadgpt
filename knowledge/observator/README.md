# Observator

CadGPT Observator is a generic CAD entity observation foundation.

The engine reads direct properties from one or many CAD entities and provides generic AppData logging primitives. It does not define HVAC/system semantics, entity filters, property projections, observation start/stop policy, or other Job behavior.

See `SPEC.md` for the V1 contract.
