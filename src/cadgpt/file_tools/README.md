# Local File Tools

Narrow GPTWorker-inspired file operations used by CadGPT. Default editable roots are only `lisp/**` and `jobs/**`.

Initial operations: list, search, read, create, and patch/edit. Path traversal and access outside configured roots must be rejected.
