# CadGPT Internal CAD Resources

Internal CAD fixtures/resources live here and version with CadGPT core. They are not User Registry Lisp capabilities and are not imported user libraries.

`CADGPT_LOAD_SMOKE.lsp` is the safe no-op fixture used by live local acceptance to verify AutoLISP loading/dispatch on an explicitly created test drawing.
