# Configuration

CadGPT configuration will define the bound local repository workspace, allowed file roots, runtime connection settings, and other non-secret defaults.

Default editable local roots are:

- `lisp/**`
- `jobs/**`
